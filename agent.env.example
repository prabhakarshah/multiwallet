# Batwa Agent Configuration Example
# Copy this file to agent.env and customize for your environment

# Unique identifier for this agent (required)
AGENT_ID=my-agent-1

# API key for authentication (optional, recommended for production)
# Generate a secure key: openssl rand -hex 32
API_KEY=your-secret-api-key-here

# Master server URL (required for auto-registration)
MASTER_URL=http://192.168.1.100:8000

# Port for this agent to listen on (default: 8001)
AGENT_PORT=8001

# Host to bind to (default: 0.0.0.0 for all interfaces)
AGENT_HOST=0.0.0.0

# Heartbeat interval in seconds (default: 30)
HEARTBEAT_INTERVAL=30
